# 使い方
## Dockerコマンド
#### 以下、rust-dockerディレクトリ直下にてコマンド実行

```$xslt
$ docker-compose build
$ docker-compose up -d
$ docker exec -it rust-docker.app /bin/bash
```

#### 実行

```$xslt
$ docker-compose up -d
```
